# 2026-03-17 记忆记录

## 项目进展：Monica代理模型路由修复

### 已完成的工作：
1. **发现了模型路由问题**：OpenClaw配置中的Monica provider包含了DeepSeek模型，导致路由混乱
2. **修复了配置问题**：
   - 从Monica provider中移除了DeepSeek相关模型（deepseek-chat, deepseek-reasoner, deepclaude）
   - 从agent models列表中移除了Monica中的DeepSeek模型映射
   - 将默认模型从`deepseek/deepseek-chat`改为`monica/gpt-5`
3. **验证了Monica代理功能**：
   - 通过curl测试确认Monica代理服务正常工作
   - Claude-4-sonnet和GPT-5模型都能正常响应
   - Monica代理后台有完整的日志输出

### 发现的问题：
1. **飞书消息路由异常**：命令行curl请求能正常到达Monica代理，但飞书消息没有日志
2. **OpenClaw配置问题**：OpenClaw可能配置了错误的API端点
3. **模型切换问题**：在飞书窗口中临时切换模型后，无法切回DeepSeek

### 关键发现：
- Monica代理服务本身功能正常
- 问题在于OpenClaw的配置和消息路由逻辑
- 需要检查OpenClaw的API端点配置是否正确指向Monica代理

### 待解决问题：
1. 飞书消息为什么没有到达Monica代理？
2. OpenClaw的API端点配置是什么？
3. 如何正确配置模型切换逻辑？

### 用户反馈的关键现象：
- 默认模型为deepseek时一切正常，Monica后台无日志
- 在飞书临时切换模型后，Monica后台有日志但消息未返回飞书
- 切换回deepseek后，仍然走Monica代理（切不回来）
- 只有在命令行执行`openclaw models set deepseek`并重启gateway后才能真正切回

### 需要进一步调查：
1. 检查OpenClaw的模型切换逻辑
2. 验证消息流格式（流式vs JSON）
3. 修复模型切换后的回退机制

---

## 重大突破：Function Calling隧道技术实现成功！

### 2026-03-17 晚：Function Calling隧道技术突破

#### 问题背景：
1. Monica代理阉割了模型的Function Calling能力
2. 模型返回的Function Calling响应被Monica过滤/转换
3. 需要绕过Monica的限制，实现Function Calling

#### 解决方案：Function Calling隧道技术
**核心思想**：在请求消息中插入"隐藏指令"，告诉模型将Function Calling响应放在特定标签内

#### 实现步骤：
1. **修改Monica代理的Custom Bot Service**：
   - 在用户消息前插入隐藏指令
   - 指令格式：`[系统指令] 请将Function Calling响应放在 <fc-tool> 标签内...`
   
2. **响应解析逻辑**：
   - 解析模型响应中的 `<fc-tool>` 标签
   - 提取JSON格式的Function Calling数据
   - 执行对应的函数
   - 返回执行结果

#### 成功验证：
1. **Claude-4-sonnet模型成功返回Function Calling标签**：
   ```
   <fc-tool>{"name": "open_browser", "arguments": {}}</fc-tool>
   ```
   
2. **Monica代理正确接收并解析**：
   - 模型路由正确：`使用Custom Bot分支处理请求`
   - 配置正确：`enable_custom_bot_mode: true, bot_uid: "gpt_5"`
   - 隧道指令已添加：`添加Function Calling隧道指令`

#### 当前状态：
✅ **Function Calling隧道技术验证成功**
- 模型正确理解了隐藏指令
- 返回了标准的Function Calling格式
- Monica代理接收到了正确的响应

⚠️ **待完成**：
- 解析逻辑需要调试（响应中仍包含原始标签）
- 需要查看完整的解析日志
- 需要验证执行结果是否正确返回给OpenClaw

#### 技术要点：
1. **隐藏指令设计**：需要在用户消息前插入，让模型知道如何返回Function Calling
2. **标签格式**：`<fc-tool>{"name": "函数名", "arguments": {}}</fc-tool>`
3. **解析逻辑**：需要正确处理Monica返回的响应结构
4. **函数执行**：根据函数名执行对应的系统命令

#### 代码提交记录：
**提交ID**: 2dab80a
**提交信息**: feat: 实现Function Calling隧道技术

**完成的功能**：
1. ✅ **Custom Bot Service增强**：在 `HandleCustomBotChat` 方法中添加Function Calling隧道支持
2. ✅ **隐藏指令插入**：在用户消息前插入系统指令，指导模型将Function Calling响应放在 `<fc-tool>` 标签内
3. ✅ **响应解析逻辑**：添加 `parseFunctionCallingTunnel` 方法，提取并解析 `<fc-tool>` 标签内容
4. ✅ **函数执行逻辑**：添加 `executeFunctionCall` 方法，根据函数名执行对应操作
5. ✅ **增强调试日志**：添加详细的类型检查和结构日志，便于问题排查
6. ✅ **辅助函数**：添加 `min` 和 `getMapKeys` 辅助函数

**已验证的功能**：
1. ✅ **模型路由正确**：`使用Custom Bot分支处理请求`
2. ✅ **配置正确加载**：`enable_custom_bot_mode: true, bot_uid: "gpt_5"`
3. ✅ **隧道指令添加**：`添加Function Calling隧道指令`
4. ✅ **模型正确响应**：Claude-4-sonnet成功返回 `<fc-tool>{"name": "open_browser", "arguments": {}}</fc-tool>`

**待验证的功能**：
1. ⚠️ **解析逻辑执行**：需要确认 `parseFunctionCallingTunnel` 方法是否正确执行
2. ⚠️ **响应类型处理**：需要确认Monica返回的响应结构是否与预期一致
3. ⚠️ **执行结果返回**：需要确认Function Calling执行结果是否正确返回给OpenClaw
4. ⚠️ **更多场景测试**：需要测试其他Function Calling场景（系统命令、天气查询等）

**关键代码文件**：
1. `/internal/service/custom_bot_service.go` - Function Calling隧道核心实现
2. `/internal/apiserver/router.go` - 增强的调试日志
3. `/internal/config/config.go` - 配置加载增强日志

#### 🎉 历史性时刻：2026-03-18 00:09 Function Calling隧道完全成功！

**提交记录**：
- **提交ID**: f72406b
- **提交信息**: fix: 修复Function Calling隧道解析逻辑

**完全成功的验证**：
```
✅ 添加Function Calling隧道指令
✅ 收到Monica原始响应
✅ 开始解析Function Calling隧道
✅ 响应是openai.ChatCompletionResponse类型
✅ 检查响应内容
✅ 找到Function Calling标签
✅ 解析到Function Calling数据
✅ 执行Function Calling
✅ Function Calling执行完成
✅ Function Calling隧道解析完成
```

**响应内容完美替换**：
- **原始响应**: `"content": "<fc-tool>{\"name\": \"open_browser\", \"arguments\": {}}</fc-tool>"`
- **解析后响应**: `"content": "已执行：打开浏览器命令"`

**技术突破**：
1. **绕过Monica限制**：通过隧道技术恢复被阉割的Function Calling
2. **隐藏指令设计**：模型正确理解并返回标准格式
3. **完整解析执行**：从标签提取到函数执行的完整流程
4. **端到端验证**：从请求到响应的完整链路验证

**里程碑意义**：
这是OpenClaw通过Monica代理实现完整AI能力的关键突破！现在可以通过Monica代理：
- 执行系统命令
- 调用各种Function Calling
- 实现完整的AI助手功能

#### 全流程测试发现问题

**测试步骤**：
1. ✅ 修改OpenClaw默认模型：`openclaw models set monica/gpt-5`
2. ✅ 系统确认模型切换成功：`Default: monica/gpt-5`
3. ❌ **Feishu消息没有显示**：发送消息后无响应
4. ❌ **Web界面有回复但不见内容**：http://127.0.0.1:18789/chat 窗口

**问题分析**：
1. **Monica代理功能正常**：curl测试返回正确JSON响应
   ```
   {"content": "我是 OpenAI 的语言模型，基于 GPT-4 架构的助手版本。"}
   ```
2. **响应方式可能有问题**：可能是流式响应（stream: true）导致前端无法解析
3. **OpenClaw请求参数问题**：可能需要检查请求中的 `stream` 参数

**已完成的调试**：
1. **增强响应方式决策日志**：提交ID 8466d2f
2. **添加user_agent字段**：便于调试OpenClaw请求
3. **确认is_openclaw逻辑**：`shouldStream := req.Stream && !isOpenClaw`

**待解决的问题**：
1. **为什么Feishu不显示消息？**
2. **OpenClaw发送的请求参数是什么？**
3. **Monica代理的响应方式决策是否正确？**

#### 明日工作计划
1. **查看OpenClaw发送的完整请求**：包括 `stream` 参数和 `User-Agent`
2. **查看Monica代理的响应决策日志**：确认 `is_openclaw` 和 `final_stream`
3. **调试流式响应问题**：确保返回JSON格式而非流式
4. **完成全流程测试**：实现通过Monica代理打开浏览器

#### 今日成就总结
✅ **Function Calling隧道技术完全实现**
✅ **模型路由逻辑正确修复**
✅ **配置加载问题解决**
✅ **响应解析执行验证成功**
⚠️ **全流程集成测试发现问题**

**今天（2026-03-17）完成了核心技术突破，明天解决集成问题！** 🎊

#### 里程碑意义：
这是实现OpenClaw通过Monica代理执行系统命令的关键突破！一旦完成，将实现：
- 通过Monica代理执行系统命令
- 完整的Function Calling支持
- 模型能力的完全释放

**今天（2026-03-17）必须完成Function Calling隧道的完整实现！**